# Database
DATABASE_URL=postgresql+asyncpg://evalhub:evalhub@localhost:5432/evalhub

# Redis
REDIS_URL=redis://localhost:6379

# Security — generate with: openssl rand -hex 32
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32

# Application
DEBUG=false
LOG_LEVEL=INFO

# ─────────────────────────────────────────
#  API Keys — add the providers you use
# ─────────────────────────────────────────

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
HUGGINGFACE_API_KEY=

# Ollama (local models) — default URL if running locally
OLLAMA_BASE_URL=http://localhost:11434
